// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
};

export const baseUrl = 'http://localhost:8088';

export const baseUrlSource = 'http://localhost:8088';

export const productUrl = 'http://localhost:8081';

export const inventoryUrl = 'http://localhost:8082';

export const payBaseUrl = 'http://localhost:8084';

export const cartUrl = 'http://localhost:8083';
//'https://learningservice-production.up.railway.app';
/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
